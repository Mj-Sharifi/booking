import axios from "axios";
import { Cookies } from "react-cookie";

const cookie = new Cookies()
// const updateUserAvatar = (,formData:FormData) => {
// const {jwt,user} = cookie.get("user_info")
 
// };
